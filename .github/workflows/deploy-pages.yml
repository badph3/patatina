name: Legacy Pages workflow (disabled)

# This workflow is intentionally kept disabled because deployment now uses
# GitHub Pages "Deploy from a branch" (main / root).
on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment mode
        run: echo "GitHub Pages deployment is configured from branch (main/root)."
